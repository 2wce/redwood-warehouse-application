# Migration `20200522142810-initial_model_setup`

This migration has been generated by Kudakwashe Mupeni at 5/22/2020, 2:28:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Product" (
"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  ,"price" TEXT NOT NULL  ,"quantity" INTEGER NOT NULL  ,"shipmentId" INTEGER   ,"sku" TEXT NOT NULL  ,FOREIGN KEY ("shipmentId") REFERENCES "Shipment"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE TABLE "quaint"."Shipment" (
"destination" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"quantity" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,"warehouseId" INTEGER NOT NULL  ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("warehouseId") REFERENCES "Warehouse"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."User" (
"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT   ,"productId" INTEGER NOT NULL  ,FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Warehouse" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"location" TEXT   ,"name" TEXT NOT NULL  ,"userId" INTEGER   ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200522142810-initial_model_setup
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,53 @@
+datasource DS {
+  provider = "sqlite"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = env("BINARY_TARGET")
+}
+
+// Define your own datamodels here and run `yarn redwood db save` to create
+// migrations for them.
+model Product {
+  id         Int       @default(autoincrement()) @id
+  name       String
+  price      String
+  quantity   Int
+  sku        String
+  createdAt  DateTime  @default(now())
+  User       User[]
+  Shipment   Shipment? @relation(fields: [shipmentId], references: [id])
+  shipmentId Int?
+}
+
+model Shipment {
+  id          Int       @default(autoincrement()) @id
+  destination String
+  quantity    Int
+  Products    Product[]
+  User        User      @relation(fields: [userId], references: [id])
+  userId      Int
+  Warehouse   Warehouse @relation(fields: [warehouseId], references: [id])
+  warehouseId Int
+}
+
+model User {
+  id         Int         @default(autoincrement()) @id
+  email      String      @unique
+  name       String?
+  Product    Product     @relation(fields: [productId], references: [id])
+  productId  Int
+  Shipments  Shipment[]
+  Warehouses Warehouse[]
+}
+
+model Warehouse {
+  id       Int        @default(autoincrement()) @id
+  name     String
+  location String?
+  Shipment Shipment[]
+  User     User?      @relation(fields: [userId], references: [id])
+  userId   Int?
+}
```


