# Migration `20200522143221-update_relations`

This migration has been generated by Kudakwashe Mupeni at 5/22/2020, 2:32:21 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200522142810-initial_model_setup..20200522143221-update_relations
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -16,38 +16,38 @@
   price      String
   quantity   Int
   sku        String
   createdAt  DateTime  @default(now())
-  User       User[]
-  Shipment   Shipment? @relation(fields: [shipmentId], references: [id])
+  user       User[]
+  shipment   Shipment? @relation(fields: [shipmentId], references: [id])
   shipmentId Int?
 }
 model Shipment {
   id          Int       @default(autoincrement()) @id
   destination String
   quantity    Int
-  Products    Product[]
-  User        User      @relation(fields: [userId], references: [id])
+  products    Product[]
+  user        User      @relation(fields: [userId], references: [id])
   userId      Int
-  Warehouse   Warehouse @relation(fields: [warehouseId], references: [id])
+  warehouse   Warehouse @relation(fields: [warehouseId], references: [id])
   warehouseId Int
 }
 model User {
   id         Int         @default(autoincrement()) @id
   email      String      @unique
   name       String?
-  Product    Product     @relation(fields: [productId], references: [id])
+  product    Product     @relation(fields: [productId], references: [id])
   productId  Int
-  Shipments  Shipment[]
-  Warehouses Warehouse[]
+  shipments  Shipment[]
+  warehouses Warehouse[]
 }
 model Warehouse {
   id       Int        @default(autoincrement()) @id
   name     String
   location String?
-  Shipment Shipment[]
-  User     User?      @relation(fields: [userId], references: [id])
+  shipment Shipment[]
+  user     User?      @relation(fields: [userId], references: [id])
   userId   Int?
 }
```


